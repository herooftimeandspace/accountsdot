# Implemented Pages Annotation Ledger

This ledger is the default landing place for annotation-driven hardening work across implemented `.pen` pages. Move a page section into its own file when the active annotation set becomes large enough to need a dedicated recovery ledger.

| ID | Page | Source | Layer | Expected fix location | Status | Durable guard |
| --- | --- | --- | --- | --- | --- | --- |
| DQ-A1 through DQ-A11 | Data Quality | `docs/design/mocks/wireframes/data-quality-annotation-ledger.md` | `.pen layout` | `docs/design/mocks/wireframes/wireframe-data-quality-dashboard.pen` | closed | Existing page ledger plus `npm run pen:lint` shared spacing/primitive checks |
| UIH-001 | All implemented pages | Design/UI hardening plan | pipeline | `scripts/sync_implemented_pages.mjs`, generated manifest, `scripts/lint_implemented_pages.mjs` | open | `npm run pen:check`, `npm run pen:lint` |
| UIH-002 | All logged-in pages | Design/UI hardening plan | `.pen layout` | shared shell/header/sidebar pattern | open | Shared shell manifest and standard refresh lint checks |
| UIH-003 | All implemented table pages | Design/UI hardening plan | `.pen layout` | authoritative `.pen` table layouts | open | Warning-only table baseline and divider-gap lint diagnostics |
| UIH-004 | All implemented pages | Design/UI hardening plan | `.pen layout` | authoritative `.pen` helper text/card/table text nodes | open | Warning-only fragmented-paragraph lint diagnostics |
| UIH-005 | All implemented pages | Design/UI hardening plan | review artifact | runtime screenshot/accessibility evidence | open | `npm run build:web`, `npm run a11y:check`, page screenshots when requested |
| UIH-107 | All logged-in implemented pages | GitHub issues #107, #108, #109, #110: shared inner-page scrolling, anchored sidebar/header, and right drawer anchored below the header without normal internal drawer scrolling | runtime behavior, docs/new behavior | `PenArtboard.jsx`, `RuntimeDrawer.jsx`, shared shell CSS, implemented-page design contract | closed | Shared renderer/CSS primitive plus browser verification on `/admin`, `/dashboard/it-admin`, `/student-data-cleanup`, `/frequent-fliers`, `/reports/security-issues`, and `/room-moves/bulk-draft`; row-detail and help drawer checks on `/frequent-fliers`; `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, and `npm run a11y:check` |
| UIH-228 | Reports | GitHub issue #228: add an IT Admin-only pending Zoom Desk Phone rename report with IncidentIQ asset-location remediation guidance | docs/new behavior, runtime behavior, DEV mock API behavior | `docs/product/product-requirements.md`, `docs/planning/implementation-plan.md`, Reports route registry/runtime page, DEV report API, route/API tests | open | Route/API authorization tests, route registry/help tests, `npm run build:web`, `npm run a11y:check`, and browser evidence on `/reports/zoom-desk-phone-renames` when the DEV server is available |
| UIH-006 | All logged-in implemented pages | User feedback: persona switcher feels broken after role change | docs/new behavior, runtime behavior | `docs/product/product-requirements.md`, `docs/planning/implementation-plan.md`, `frontend/src/app.jsx` | closed | Docs update plus `npm run build:web`, `npm run a11y:check`, and in-app browser verification of DEV persona-switch routing and strict direct-link `403` behavior |
| UIH-007 | All logged-in implemented pages | User-edited Data Quality shell should become standalone shared shell authority | pipeline, `.pen layout` | `wireframe-shared-shell.pen`, `scripts/sync_implemented_pages.mjs`, generated artboards | closed | `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, Pencil layout snapshots, and browser verification on `/data-quality`, `/phone-directory/by-person`, and logged-in `/error/403` |
| UIH-008 | All logged-in implemented pages | User feedback: shared-shell tagline should stay Vegas Gold even when contrast tooling objects | docs/new behavior, `.pen layout`, runtime behavior | `wireframe-shared-shell.pen`, generated artboards, `scripts/check_frontend_accessibility.mjs` | closed | PRD/plan override record plus narrow accessibility-check exception for exact tagline and browser verification |
| UIH-009 | IT Admin Dashboard | User feedback: dashboard overview should use shared shell/refresh primitives and reduce split table text boxes | pipeline, `.pen layout` | `wireframe-it-admin-overview.pen`, generated dashboard artboard | closed | `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, Pencil layout snapshot, and browser verification on `/dashboard/it-admin` |
| UIH-010 | IT Admin Dashboard | User feedback: Schedules Running Next status badges overlap the card border | `.pen layout` | `wireframe-it-admin-overview.pen`, generated dashboard artboard | closed | Status badge column resize in the authoritative PEN plus `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and in-app browser verification on `/dashboard/it-admin` |
| UIH-011 | Onboarding | User feedback: cleanup pass should use shared shell/refresh primitives and reduce fragmented table text, spacing collisions, and badge overflow | pipeline, `.pen layout` | `wireframe-onboarding-dashboard.pen`, generated onboarding artboard | closed | Page-pane cleanup in the authoritative PEN plus `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and in-app browser verification on `/onboarding` |
| UIH-012 | Onboarding | User feedback: replace fixed Selected Workflow overlay with a row-click right-hand drawer, delete helper callout, and expand the table into the freed pane space | docs/new behavior, runtime behavior, `.pen layout` | `docs/product/product-requirements.md`, `docs/planning/implementation-plan.md`, `StaticPenPage.jsx`, `wireframe-onboarding-dashboard.pen`, generated onboarding artboard | closed | PRD/plan behavior note plus row-click/update/close browser verification, `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, and `npm run a11y:check` |
| UIH-013 | All row-detail implemented pages | User feedback: right-hand drawer should become a reusable runtime primitive and replace static row detail/context panels | docs/new behavior, runtime behavior, pipeline, `.pen layout` | `implemented-page-design-contract.md`, `docs/product/product-requirements.md`, `docs/planning/implementation-plan.md`, shared drawer component, affected `.pen` files, generated artboards | completed | Shared drawer primitive plus row-click/update/close browser verification, `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, and `npm run a11y:check` |
| UIH-014 | Onboarding and shared shell | User feedback: add Non-Escape manual intake drawer, rename sidebar item to Onboarding, remove Current Rules/helper note, pin table footer, and move Start to first table column | docs/new behavior, runtime behavior, DEV mock API behavior, `.pen layout`, pipeline | `docs/product/product-requirements.md`, `docs/planning/implementation-plan.md`, `wireframe-shared-shell.pen`, `wireframe-onboarding-dashboard.pen`, generated artboards, Onboarding runtime page, DEV onboarding APIs | completed | Docs update plus `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, Go handler tests, `npm run build:web`, `npm run a11y:check`, and browser verification on `/onboarding` |
| UIH-015 | Onboarding | User feedback: add Date Added as the first onboarding table column, make Add Non-Escape reliably open the drawer, compact drawer field rows, move missing required fields into the status bubble, use brand red missing-field borders, and show workflow action details | docs/new behavior, runtime behavior, DEV mock API behavior, `.pen layout` | `docs/product/product-requirements.md`, `docs/planning/implementation-plan.md`, `wireframe-onboarding-dashboard.pen`, generated onboarding artboard, Onboarding runtime page, DEV onboarding APIs | completed | Docs update plus `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, Go handler tests, `npm run build:web`, `npm run a11y:check`, and browser verification on `/onboarding` |
| UIH-016 | Onboarding | Regression feedback: Add Non-Escape visual button could appear without a dependable runtime-owned click target | runtime behavior, `.pen layout` | Onboarding runtime page and runtime button styling | completed | Single visible runtime-owned Vegas Gold button, static PEN button hidden in live page, `npm run build:web`, `npm run a11y:check`, and browser verification on `/onboarding` |
| UIH-017 | Onboarding | User feedback: provide a deterministic onboarding table entry where Start Date is within three days of Date Added for reviewing the manual intake lead-time warning | runtime behavior, DEV mock API behavior | DEV onboarding API seed data and handler tests | completed | Deterministic editable manual draft row plus Go handler test, `npm run build:web`, and browser verification of the warning icon/title on `/onboarding` |
| UIH-018 | Logged-in error pages and Onboarding | User feedback: error endpoints show floating text in the shell, onboarding lead-time warning needs hover text, table start date format should match other dates, and table rows should show the warning icon | runtime behavior, `.pen layout` | Error runtime overlay, Onboarding runtime table/drawer warning component, DEV onboarding row payload | completed | Runtime-owned logged-in error copy, reusable hover/focus warning tooltip, formatted table dates, row warning icon, Go handler test, `npm run build:web`, `npm run a11y:check`, and browser verification |
| UIH-019 | All implemented pages | User feedback: inventory reused status bubbles/buttons and define reusable severity colors before updating badge colors, including making `Blocked` brand red with accessible text | docs/new behavior | `implemented-page-design-contract.md` status badge/button color inventory | completed | Shared primitive inventory table mapping reused labels to brand color tokens and identifying the follow-up cleanup target for a shared status badge primitive |
| UIH-020 | Login and error pages | User directive: any text rendered with the Varsity font must be lowercase globally | docs/new behavior, `.pen layout`, runtime behavior | Design contract, login/error PEN files, generated login/error artboards, error runtime copy | completed | Shared Varsity display-text primitive rule plus `npm run pen:sync`, `npm run pen:check`, `npm run build:web`, and `npm run a11y:check` |
| UIH-021 | Onboarding and shared shell | User feedback: warning icon must not wrap the start-date text, the help icon should open a right-hand drawer with current-page end-user documentation, and status badge colors should follow the reviewed severity palette | docs/new behavior, runtime behavior, `.pen layout` | Shared help icon overlay, shared drawer primitive, Onboarding runtime table/status styles, design contract status inventory | completed | Runtime-owned help hotspot/drawer, non-wrapping start-date warning cell, severity status classes, `npm run build:web`, `npm run a11y:check`, and browser verification on `/onboarding` |
| UIH-022 | All shared-shell pages and Onboarding | User feedback: help drawer behavior should apply wherever the help icon is visible, warning tooltips must not be clipped, blocked workflow drawers should identify missing fields, and room-mapping workflows need IncidentIQ site/room override controls | docs/new behavior, runtime behavior, DEV mock API behavior | Shared help overlay defaults, Onboarding runtime warning tooltip, Onboarding workflow drawer, DEV onboarding seed copy | completed | Default help drawer content for every shared-shell help icon, drawer-contained warning tooltip placement, missing-field callout, IncidentIQ site/room filter controls, `npm run build:web`, and `npm run a11y:check` |
| UIH-023 | Offboarding | User feedback: remove the title-adjacent gold pill, Deprovision Scope card, and bottom helper note; reflow Offboarding cards and tables into the freed pane space | pipeline, `.pen layout` | `wireframe-offboarding-dashboard.pen`, generated Offboarding artboard | completed | `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/offboarding` |
| UIH-024 | Offboarding | User feedback: rows should open a right-hand drawer; fixed Queue Actions should become per-row drawer actions; email and role-scoped employee ID should appear in the table; Escape-backed end dates should be read-only while non-Escape/orphan end dates can be edited by HR/IT | docs/new behavior, runtime behavior, DEV mock API behavior, `.pen layout`, pipeline | Offboarding DEV API, `OffboardingPage.jsx`, `wireframe-offboarding-dashboard.pen`, generated Offboarding artboard | completed | Docs update plus `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, Go handler tests, `npm run build:web`, `npm run a11y:check`, and browser verification on `/offboarding` |
| UIH-104 | Offboarding | GitHub issue #161: add HR/IT-only Emergency Offboarding and Offboard Contractor actions that open the shared right-hand drawer and schedule DEV mock deprovision work without live provider writes; reopened Browser feedback requires the two top-level buttons to stack vertically immediately next to the summary info boxes | docs/new behavior, runtime behavior, DEV mock API behavior | `docs/product/product-requirements.md`, `docs/planning/implementation-plan.md`, `docs/planning/external-write-inventory.md`, `frontend/src/pages/OffboardingPage.jsx`, `internal/web/dev_offboarding.go`, `frontend/src/styles.css` | in progress | Server-side HR/IT authorization on candidate and schedule APIs, DEV mock-only write inventory entry, focused Go handler tests, adjacent vertical action-group CSS, `npm run build:web`, `npm run a11y:check`, and Browser evidence on `/offboarding` for Human Resources and IT Admin |
| UIH-025 | All runtime drawer pages | User feedback: clicking blank page space should close an open drawer wherever the shared drawer primitive is used, while row clicks, shell controls, and drawer controls remain interactive; the Offboarding end-date picker and save button should sit on one row | runtime behavior | Shared drawer primitive, Offboarding runtime styles | completed | Shared outside-click guard excluding drawer and interactive controls plus `npm run build:web`, `npm run a11y:check`, and browser verification on `/offboarding` |
| UIH-026 | All runtime table pages | User feedback: table headers should support three-way sort, tables should expose local search/filter by available row data, and each page should be able to define its default sort column | docs/new behavior, runtime behavior | Shared runtime table-control primitive plus Onboarding, Offboarding, Phone Directory, and Data Quality runtime/semantic tables | completed | Shared search/sort helper, page-specific default sort configs, role-scoped row data filtering, `npm run build:web`, `npm run a11y:check`, and runtime browser verification |
| UIH-027 | Departing Seniors | User feedback: add a sidebar page for current senior-class account deprovision review with end-date overrides, IncidentIQ device visibility, deprovision actions, and broad row search | docs/new behavior, runtime behavior, DEV mock API behavior | PRD/implementation docs, DEV persona routes/API handlers, Departing Seniors runtime page, shared shell sidebar metadata | completed | DEV mock handler tests plus shared runtime table search/sort, `npm run build:web`, `npm run a11y:check`, and browser verification on `/departing-seniors` |
| UIH-084 | Departing Seniors | GitHub issue #43: move device-return notes into the shared row drawer, remove visible header description and Grad Year table column, show IncidentIQ serial links only for real assets, and expose retained school-year selection | docs/new behavior, runtime behavior, DEV mock API behavior | PRD/implementation docs, DEV Departing Seniors API payload/tests, Departing Seniors runtime page, shared drawer primitive | closed | DEV mock handler tests for school-year options, retention, and asset URLs; shared RuntimeDrawer behavior; `npm run build:web`, `npm run a11y:check`, and headless screenshot evidence on `/departing-seniors` |
| UIH-028 | Offboarding | User feedback: license reclamation drawer copy should name the specific licenses being reclaimed | runtime behavior, DEV mock API behavior | Offboarding DEV mock row action/details payload | completed | Handler test coverage for named license details plus `npm run build:web` |
| UIH-029 | Offboarding | User feedback: shared table search shifted rows below the existing table card border | runtime behavior | Offboarding runtime table wrapper | completed | Runtime-owned table card border sized for search plus `npm run build:web`, `npm run a11y:check`, and browser verification on `/offboarding` |
| UIH-030 | Room Moves | User feedback: run the standard artboard cleanup pass while preserving shared shell/refresh primitives and reducing fragmented table/helper text plus divider collisions | pipeline, `.pen layout` | `wireframe-room-moves.pen`, generated Room Moves artboard | completed | `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves` |
| UIH-031 | Phone Directory | User feedback: run the standard artboard cleanup pass across By Person, By Room, and By Department while preserving shared shell/refresh primitives and reducing fragmented table/helper text plus spacing collisions | pipeline, `.pen layout` | `wireframe-phone-directory-by-person.pen`, `wireframe-phone-directory-by-room.pen`, `wireframe-phone-directory-by-department.pen`, generated Phone Directory artboards | completed | `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/phone-directory`, `/phone-directory/by-person`, `/phone-directory/by-room`, and `/phone-directory/by-department` |
| UIH-032 | Data Quality | User feedback: run the standard artboard cleanup pass while preserving shared shell/refresh primitives and reducing fragmented table text plus spacing collisions | pipeline, `.pen layout` | `wireframe-data-quality-dashboard.pen`, generated Data Quality artboard | completed | Explicit table-cell text widths plus `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/data-quality` |
| UIH-033 | Frequent Fliers | User feedback: run the standard artboard cleanup pass while preserving shared shell/refresh primitives and reducing fragmented table/helper text plus divider collisions | pipeline, `.pen layout` | `wireframe-device-wrangler-frequent-fliers.pen`, generated Frequent Fliers artboard | completed | Row-baseline cleanup, combined split detail text, `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/frequent-fliers` |
| UIH-034 | Student Data Cleanup | User feedback: run the standard artboard cleanup pass while preserving shared shell/refresh primitives and reducing fragmented table text plus divider collisions | pipeline, `.pen layout` | `wireframe-site-secretary-student-data-cleanup.pen`, generated Student Data Cleanup artboard | completed | Combined split timestamp/name cells, row-baseline cleanup, `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/student-data-cleanup` |
| UIH-035 | Reports | User feedback: run the standard artboard cleanup pass while preserving shared shell/refresh primitives and reducing fragmented table/detail text plus divider collisions | pipeline, `.pen layout` | `wireframe-it-admin-reports.pen`, generated Reports artboard | completed | Combined split detail/table cells, row-baseline cleanup, `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/reports` |
| UIH-036 | Admin | User feedback: run the standard artboard cleanup pass while preserving shared shell/refresh primitives and reducing fragmented table/card text plus divider collisions | pipeline, `.pen layout` | `wireframe-it-admin-admin-controls.pen`, generated Admin artboard | completed | Combined split warning/exception/table cells, row-baseline cleanup, `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/admin` |
| UIH-037 | All logged-in implemented pages | User feedback: DEV persona switcher appeared stuck when switching from a persona-specific dashboard because the session changed but the current allowed dashboard route stayed in place | runtime behavior | DEV persona switch routing in `frontend/src/app.jsx` | completed | Dashboard-to-dashboard persona switches now route to the new persona landing dashboard while direct-link authorization remains unchanged; `npm run build:web`, `npm run a11y:check`, and browser verification |
| UIH-038 | All logged-in implemented pages | User feedback: corrected dashboard persona switching feels slower because the previous page remains visible while the target persona dashboard mounts | runtime behavior | DEV persona switch state, switcher status copy, and switching overlay | completed | Dedicated persona switch state keeps session ready, updates the select immediately, announces the target persona, and shows a lightweight overlay; `npm run build:web`, `npm run a11y:check`, and browser verification |
| UIH-039 | Phone Directory | User feedback: Phone Directory should always return district-wide people, room, and department results while letting site-level users see their assigned site first; remove page-local filter strip and implementation-detail subtitles/notes | docs/new behavior, runtime behavior, DEV mock API behavior | PRD/implementation docs, DEV Phone Directory API payload/ranking, `PhoneDirectoryPage.jsx` runtime table overlays | completed | Directory focus separated from shared-header site scope, all modes return district rows with optional site boost, Go handler tests, `npm run build:web`, `npm run a11y:check`, and browser verification on all Phone Directory modes |
| UIH-040 | Phone Directory | User feedback: functional directory scope dropdown should not show static shell text underneath, and the runtime table should move into the freed filter-strip space and grow vertically to reveal all district rows | runtime behavior | `PhoneDirectoryPage.jsx` runtime shell/table overlays | completed | Runtime hides static shared-shell scope nodes behind the live dropdown, hides the old static table frame, expands Phone Directory artboard height from row count, and browser verification on `/phone-directory/by-person` |
| UIH-041 | All logged-in implemented pages | User feedback: shared header search is a global search entrypoint, not a Phone Directory-only shortcut; numeric extension searches must not leave Phone Directory stuck loading | docs/new behavior, runtime behavior, DEV mock API behavior | PRD/implementation docs, DEV global search API, `/search` runtime page, shared header search routing, Phone Directory request lifecycle | completed | Global search route/API groups accessible results without leaking hidden employee IDs, header submit routes to `/search?q=...`, Phone Directory request keys always settle loading, Go handler tests, `npm run build:web`, `npm run a11y:check`, and browser verification |
| UIH-042 | Frequent Fliers | User feedback: move the fixed student detail panel into the shared drawer, make threshold/type filters real controls, replace static trend marks with a color-coded graph, move helper copy into global help, and make device/ticket rows link to deterministic DEV targets | docs/new behavior, runtime behavior | PRD/implementation docs, `FrequentFliersPage.jsx`, shared drawer/help/table primitives | completed | Runtime-owned Frequent Fliers pane over the shared shell plus `npm run build:web`, `npm run a11y:check`, and browser verification on `/frequent-fliers` |
| UIH-043 | Student Data Cleanup | User feedback: fix the Sync button overflow/color, move helper copy into page help, remove static filter/helper clutter, and use page-level search/filter plus shared table sorting and auto-sized rows | docs/new behavior, runtime behavior | PRD/implementation docs, `StudentDataCleanupPage.jsx`, shared help/table/drawer primitives | completed | Runtime-owned Student Data Cleanup pane over the shared shell plus `npm run build:web`, `npm run a11y:check`, and browser verification on `/student-data-cleanup` |
| UIH-044 | Reports | User feedback: move the fixed report detail card into the shared right-hand drawer, and make every report or refresh row open relevant row details through the shared open/close drawer primitive | docs/new behavior, runtime behavior | PRD/implementation docs, `ReportsPage.jsx`, shared drawer/table primitives | completed | Runtime-owned Reports pane over the shared shell plus `npm run build:web`, `npm run a11y:check`, and browser verification on `/reports` |
| UIH-045 | Student Data Cleanup | User feedback: compare `Student_Aeries_AD_Sync.ps1` and ensure DEV mock rows cover every invalid-name variation the legacy checker catches or resolves | docs/new behavior, runtime behavior | PRD/implementation docs, `StudentDataCleanupPage.jsx` DEV mock rows | completed | Mock rows now cover diacritics, apostrophes, smart punctuation, hyphens, digits, symbols, and leading/trailing first/last-name whitespace plus `npm run build:web`, `npm run a11y:check`, and browser verification on `/student-data-cleanup` |
| UIH-046 | All logged-in implemented pages | User feedback: header scope/site dropdown should become a shared runtime primitive, and Phone Directory should keep its existing directory-focus behavior through that primitive | docs/new behavior, runtime behavior | Shared shell scope dropdown primitive, `PhoneDirectoryPage.jsx`, PRD/implementation docs | completed | Shared dropdown style/behavior update plus `npm run build:web`, `npm run a11y:check`, and browser verification on `/student-data-cleanup`, `/phone-directory/by-person`, and `/admin` |
| UIH-047 | Admin | User feedback: remove the title-adjacent empty gold pill and run the Admin Controls artboard cleanup pass without changing shared shell authority | pipeline, `.pen layout` | `wireframe-it-admin-admin-controls.pen`, generated Admin artboard | completed | Source `.pen` cleanup plus `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/admin` |
| UIH-048 | Room Moves | User feedback: make Room Moves runtime-backed with single-move drawer drafts, whole-site and manually built bulk draft modes, scoped destination-site and room controls, warning placement, and help-drawer cutover guidance | docs/new behavior, `.pen layout`, runtime behavior, DEV mock API behavior, pipeline | PRD/implementation docs, Room Moves DEV APIs, `RoomMovesPage.jsx`, `wireframe-room-moves.pen`, `wireframe-room-moves-bulk-draft.pen`, generated Room Moves artboards | completed | `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, Go handler tests, `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves` and `/room-moves/bulk-draft` |
| UIH-049 | Room Moves | User feedback: runtime action buttons should not duplicate or overflow, and the single-move drawer should use the shared drawer layering with a cancel action that deletes any saved draft before closing | runtime behavior | `RoomMovesPage.jsx`, shared runtime drawer z-index, Room Moves runtime action styling | completed | `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves` |
| UIH-203 | Room Moves | GitHub issue #203: right drawer footer action buttons should use the available drawer width | runtime behavior | Shared right-drawer footer action-row layout (`frontend/src/styles.css`) | completed | Drawer footer actions use auto-sized columns so 3+ actions fill the full footer width; guard with `npm run build:web` and browser verification on `/room-moves` right drawer |
| UIH-050 | Room Moves | User feedback: clicking either bulk room button should land on a usable bulk draft page instead of an apparently empty surface | runtime behavior | `RoomMovesPage.jsx`, Room Moves runtime action styling | completed | Bulk action buttons hide static duplicates by artboard prefix, stay within the artboard, and manual build-list drafts show an explicit empty state plus `npm run build:web` and `npm run a11y:check` |
| UIH-051 | Room Moves | User feedback: clicking the X on the Room Move drawer should close the drawer without deleting any saved draft; Cancel remains the destructive cleanup affordance | runtime behavior | Shared drawer close button primitive, Room Moves drawer cancel semantics | completed | Shared drawer X handles pointer and keyboard close without calling draft deletion plus `npm run build:web` and `npm run a11y:check` |
| UIH-052 | Room Moves, Admin | User feedback: every pending Room Moves row needs a `Cancel Move` affordance until the job runs, and IT Admin needs an Admin-only completed-job reversal surface with explicit full-job confirmation guidance | docs/new behavior, runtime behavior, DEV mock API behavior | PRD/implementation docs, Room Moves DEV APIs, `RoomMovesPage.jsx`, Admin runtime overlay, Room Moves help drawer | completed | Go handler tests, `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves`, `/room-moves/bulk-draft`, and `/admin` |
| UIH-053 | Room Moves | User feedback: the bulk room-move draft page should hide obsolete summary boxes, and all Room Moves room selectors should show unique room values with a single `None` option first while preserving the current-room default | runtime behavior, DEV mock API behavior, pipeline | Room Moves DEV APIs, `RoomMovesPage.jsx`, generated bulk-draft static-node suppression | completed | Go handler tests, `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves/bulk-draft` |
| UIH-054 | Room Moves | User feedback: remove the leftover one-person correction helper copy into the help drawer context, keep runtime action buttons visible, and widen the Move Set Review table across the available page width | runtime behavior, pipeline | `RoomMovesPage.jsx`, generated Room Moves static-node suppression | completed | `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves` |
| UIH-055 | Room Moves | User feedback: Move Set Review must include persona-visible scheduled bulk moves, use `Bulk Move` as the bulk-row name, move warnings out of the table, and add move author immediately before State for both single and bulk rows | docs/new behavior, runtime behavior, DEV mock API behavior | PRD/implementation docs, Room Moves DEV row payload, `RoomMovesPage.jsx` | completed | Go handler tests, `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves` |
| UIH-056 | Room Moves | User feedback: IT Admin bulk-row clicks must open visible drafts authored by any operator, and Room Moves action buttons must not overlap the Batch Cutovers card | docs/new behavior, runtime behavior, DEV mock API behavior | PRD/implementation docs, Room Moves DEV bulk-draft payload, `RoomMovesPage.jsx` route-payload guard and button placement | completed | Go handler tests, `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves` bulk-row navigation |
| UIH-057 | Room Moves | User feedback: the Jamie Reed empty-room phone outcome should not require review; the documented null-room automation path removes phone and SLG assignments and converts the source room phone to common area when this user is the last active room user | docs/new behavior, runtime behavior, DEV mock API behavior | PRD/implementation docs, Room Moves DEV seed data, and handler tests | completed | Go handler tests, `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves` |
| UIH-058 | Room Moves | User feedback: rename action buttons to `Move Person`, `Batch Move`, and `Site Rollover`; `Batch Move` opens the blank bulk draft, `Site Rollover` opens the preloaded roster draft, and the action group needs more right-side clearance | docs/new behavior, runtime behavior | PRD/implementation docs, `RoomMovesPage.jsx` action labels, mode mapping, and position | completed | `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves` |
| UIH-059 | Shared Drawer, Room Moves | User feedback: the `Move Person` drawer must inherit the shared right-drawer primitive positioning and must not render over the DEV persona switcher | runtime behavior | Shared `RuntimeDrawer` DEV toolbar offset and Room Moves drawer title | completed | `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves` |
| UIH-060 | Room Moves | User feedback: remove the growing person result list from the `Move Person` drawer and use the scoped search input itself for autocomplete selection | runtime behavior | `RoomMovesPage.jsx` person autocomplete control | completed | `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves` |
| UIH-061 | Room Moves | User feedback: bulk draft page title should match the button that opened it, showing `Site Rollover` for roster drafts and `Batch Move` for manual-list drafts | runtime behavior, DEV mock API behavior | Room Moves bulk draft payload title and generated title text override | completed | Go handler tests, `npm run build:web`, `npm run a11y:check`, and browser verification on both bulk draft modes |
| UIH-062 | Room Moves | User feedback: bulk draft explanatory subtitle belongs in the page help drawer, and the redundant bulk mode label above the scoped site should be removed so the site context shifts up | runtime behavior, pipeline | Room Moves help drawer copy, generated bulk-draft subtitle suppression, and `RoomMovesPage.jsx` bulk toolbar | completed | `npm run build:web`, `npm run a11y:check`, and browser verification on both bulk draft modes |
| UIH-063 | Room Moves | User feedback: bulk draft header, effective date, dropdowns, and row action buttons should align to the current runtime table/button primitives, remove the visible school name, and close the whitespace above the table controls | runtime behavior | `RoomMovesPage.jsx` bulk draft runtime table/header and Room Moves CSS table/button primitives | completed | `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves/bulk-draft?draft_id=rm-draft-101` and `/room-moves/bulk-draft?draft_id=rm-draft-103` |
| UIH-064 | Room Moves | User feedback: the bulk draft header/search section should use 100% of the runtime page width without empty right-side whitespace | runtime behavior | Room Moves bulk draft titlebar/table-tools CSS primitives | completed | `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves/bulk-draft?draft_id=rm-draft-103` |
| UIH-065 | All logged-in implemented pages | User feedback: the DEV persona switcher should collapse by default so it does not cover annotation targets, while preserving persona switching and status announcements | runtime behavior | DEV persona switcher shared-shell tooling primitive | completed | `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves/bulk-draft?draft_id=rm-draft-103` |
| UIH-066 | Room Moves | User feedback: bulk draft pages should not show the shared refresh button or last-refreshed metadata, and should reclaim the unused header whitespace | runtime behavior | Room Moves bulk draft refresh primitive suppression and runtime header spacing | completed | `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves/bulk-draft?draft_id=rm-draft-101`, `/room-moves/bulk-draft?draft_id=rm-draft-103`, and `/room-moves` |
| UIH-067 | All logged-in implemented pages | User feedback: the expanded DEV persona switcher should remove explanatory text and redundant DEV eyebrow copy, keep a non-wrapping `Dev-only Persona Switcher` title, and close the leftover whitespace | runtime behavior | DEV persona switcher shared-shell tooling primitive | completed | `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves/bulk-draft?draft_id=rm-draft-103` |
| UIH-068 | All logged-in implemented pages | User feedback: DEV performance investigation needs a non-redundant route-transition matrix plus per-route refresh samples because Browser hangs are most visible after code-change refreshes | runtime behavior | Shared shell routing and Browser refresh performance harness | completed | Matrix dry-run coverage validation, Browser-skill matrix sample/run, `npm run build:web`, `npm run a11y:check`, and refresh verification on `/departing-seniors` |
| UIH-069 | Feature Flags | User feedback: `/admin/feature-flags` must be a separate implemented subpage with its own authoritative PEN source and no shared refresh button or last-refreshed text | pipeline, `.pen layout`, runtime behavior | `wireframe-admin-feature-flags.pen`, generated `admin-feature-flags` artboard, Feature Flags runtime page refresh opt-out | completed | `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/admin/feature-flags` |
| UIH-070 | All implemented pages, Room Moves bulk draft | User feedback: generated artboard JSON should load per route, and annotation-driven frontend cleanup must update authoritative PEN sources instead of hiding obsolete generated nodes | pipeline, `.pen layout` | Artboard sync pipeline, generated artboard loader helper, `wireframe-room-moves-bulk-draft.pen`, and image-fill validation | completed | Per-route dynamic artboard loaders, Room Moves bulk draft PEN cleanup, image source/public asset checks in `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/room-moves`, `/room-moves/bulk-draft?draft_id=rm-draft-101`, and `/room-moves/bulk-draft?draft_id=rm-draft-103` |
| UIH-071 | Feature Flags | User feedback: remove static Feature Flags scaffold rows and placeholder labels from the authoritative PEN instead of hiding those nodes in runtime code | `.pen layout`, runtime behavior | `wireframe-admin-feature-flags.pen`, generated `admin-feature-flags` artboard, `FeatureFlagsPage.jsx` static-node suppression cleanup | completed | Minimal source PEN page pane plus generated-artboard string check, `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, and `npm run a11y:check` |
| UIH-072 | All implemented pages | GitHub issue #14: group warning-only design lint diagnostics by primitive and promote stable checks where safe | pipeline | `scripts/lint_implemented_pages.mjs` | completed | `npm run pen:lint` now groups warnings by `table`, `helper paragraph`, `wrapper/card/rail`, and page-local exceptions; text overflow is promoted to a blocking failure after the parent-relative coordinate fix and guarded by `npm run pen:lint:test` |
| UIH-073 | Login | GitHub issue #14: `login` text-overflow warning for `t7` inside `f5` was a lint coordinate false positive, not a `.pen` layout defect | pipeline | `scripts/lint_implemented_pages.mjs` | accepted exception | Linter geometry now compares nested child nodes using absolute coordinates, so parent-relative login button text is accepted only when it remains inside the parent frame; the self-test covers passing nested text and real nested overflow |
| UIH-074 | All implemented table pages | GitHub issue #14: remaining table-baseline warnings need page-level `.pen` review after duplicate-shell cleanup lands | `.pen layout` | affected authoritative table `.pen` files | open | Deferred to the issue #12 integration pass because issue #12 already edits the relevant `.pen` files broadly; grouped `table` warnings in `npm run pen:lint` are the durable triage guard |
| UIH-075 | Dashboard Site Admin, Reports Ticketing Human Work, My Profile | GitHub issue #14: fragmented helper-paragraph warnings identify likely multi-node paragraph cleanup candidates | `.pen layout` | `wireframe-site-admin-dashboard.pen`, `wireframe-ticketing-human-work.pen`, `wireframe-faculty-staff-my-profile.pen` | open | Deferred to the issue #12 integration pass to avoid duplicating active `.pen` edits; grouped `helper paragraph` warnings in `npm run pen:lint` keep the candidate list visible |
| UIH-076 | Phone Directory by Department | GitHub issue #14: bordered-wrapper warnings identify department summary/table wrapper spacing candidates | `.pen layout` | `wireframe-phone-directory-by-department.pen` | open | Deferred to the issue #12 integration pass to avoid conflicting with active source `.pen` cleanup; grouped `wrapper/card/rail` warnings in `npm run pen:lint` keep the page-local candidates visible |
| UIH-077 | Frequent Fliers, Offboarding, Onboarding, Phone Directory, Reports, Room Moves, Student Data Cleanup | Issue #11: remaining page-local generated nodes are hidden at runtime after those surfaces moved static panes, tables, buttons, filters, and detail rails into runtime-owned controls | runtime behavior, `.pen layout` | `docs/design/mocks/wireframes/hidden-generated-node-ledger.md` plus the listed page runtime files and authoritative `.pen` sources | completed | Dedicated hidden generated-node ledger plus `npm run pen:lint` enforcement that every assigned page with page-local generated suppression has a documented exception |
| UIH-078 | Phone Directory | GitHub issue #56: By Room and By Department should drop redundant `Details` columns, all modes should order Phone before Extension, and phone/extension values should be `tel:` links | runtime behavior, docs/new behavior | `PhoneDirectoryPage.jsx`, Phone Directory CSS grid columns, `docs/product/product-requirements.md`, `docs/planning/implementation-plan.md` | completed | Runtime table columns own the live Phone Directory results while generated table nodes remain hidden; docs record the `tel:<digits>` internal extension format, build/a11y checks verify the patch, and issue #56 PR evidence captures browser/headless verification paths |
| UIH-079 | Student Data Cleanup | GitHub issue #60: name-correction fields use technical `raw` and `clean` labels, the Aeries action implies a student-record deep link, and the runtime table leaves avoidable right-side whitespace | docs/new behavior, runtime behavior | `docs/product/product-requirements.md`, `docs/planning/implementation-plan.md`, `StudentDataCleanupPage.jsx`, `frontend/src/styles.css` | completed | PRD/plan wording requires current/suggested labels while preserving source-system values, runtime table/drawer labels avoid raw/clean terminology, the Aeries action links to the base site, #59 keeps shared sync/refresh primitive work out of this page-local pass, and verification includes `npm run build:web`, `npm run a11y:check`, `npm run pen:check`, `npm run pen:lint`, and browser screenshots |
| UIH-080 | Shared Drawer | Issue #40: unbounded shared right drawers, including Onboarding manual intake and shared help drawers, render near the middle of the artboard because the DEV toolbar offset moves the primitive away from the right edge while bounded row drawers stay correctly placed | runtime behavior | Shared `RuntimeDrawer` placement styles | completed | Shared right-drawer primitive style guard plus headless Chrome verification on `/onboarding`, `/student-data-cleanup`, and `/reports` with PNG evidence in `artifacts/issues/issue-40/`; `npm run build:web`, `npm run a11y:check`, `npm run pen:check`, `npm run pen:lint`, and `git diff --check` |
| UIH-081 | Room Moves | GitHub issue #54: Morgan Lee-like primary-room conflicts need L1-operable drawer context and should resolve through shared-line-group automation rather than generic manual-ticket wording | docs/new behavior, runtime behavior, DEV mock API behavior, pipeline | `docs/product/product-requirements.md`, `docs/planning/implementation-plan.md`, Room Moves DEV API rows, `RoomMovesPage.jsx` shared drawer detail fields | completed | PRD/plan primary-conflict behavior text, structured DEV row detail fields, Go handler tests, `npm run build:web`, `npm run a11y:check`, and headless-browser evidence at `artifacts/issues/issue-54/room-moves-primary-conflict.png` |
| UIH-082 | Admin | GitHub issue #61: clean up Admin artboard alignment, remove invalid zero-width artboard artifacts, and reserve visible page space so the Admin-only Room Move Reversal overlay no longer collides with lower Admin cards | pipeline, `.pen layout` | `wireframe-it-admin-admin-controls.pen`, generated Admin artboard | closed | Source `.pen` cleanup, generated artboard bounds preservation, `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, `git diff --check`, and headless IT Admin `/admin` evidence at `artifacts/issues/issue-61/admin-it-admin-headless.png`; shared refresh/sync primitive changes remain tracked in issue #59 |
| UIH-083 | Data Quality | GitHub issue #57: move routing/helper copy into the shared help drawer and validate the `Open Mapping Dashboard` affordance before keeping it | docs/new behavior, `.pen layout`, runtime behavior | `wireframe-data-quality-dashboard.pen`, Data Quality runtime page, shared help copy, and sync-dashboard walkthrough docs | completed | Product docs do not list a supported `/data-quality` mapping-dashboard route or workflow, so the speculative button/hotspot was removed, queue-routing guidance moved to help, and the fix is guarded by `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, headless `/data-quality` screenshots in `artifacts/issues/issue-57/`, and `git diff --check` |
| UIH-084 | Login and error pages | GitHub issue #53: `/login` app-level request failures rendered a `500` error headline while the request detail reported a different HTTP status such as `404` | runtime behavior | App-level error status normalization shared by session, login, and logout request failures | completed | Node regression test for status normalization plus `npm run build:web`, `npm run a11y:check`, and browser verification on `/login`, `/error/404`, and `/error/500` |
| UIH-085 | Room Moves bulk draft | GitHub issue #55: bulk draft table should merge email into the person column, remove bulk refresh, align Add with search, and preserve add/removal row-clearing semantics after reload | `.pen layout`, runtime behavior, DEV mock API behavior | `wireframe-room-moves-bulk-draft.pen`, Room Moves runtime bulk table, DEV Room Moves draft normalization | closed | Source PEN refresh removal, runtime table primitive update, handler tests for add/removal serialization, `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser evidence for `/room-moves/bulk-draft?draft_id=rm-draft-103` |
| UIH-099 | Room Moves | GitHub issues #54, #134, #135, and #137: current ui-improvements feedback asks Room Moves to remove automated-path external-system drawer clutter, add the Scheduled review-table column, rename Phone to Action(s), remove the visible Move Set Review heading, and show person-specific bulk warning bullets | docs/new behavior, `.pen layout`, runtime behavior, DEV mock API behavior, pipeline | `docs/product/product-requirements.md`, `docs/planning/implementation-plan.md`, `wireframe-room-moves.pen`, `RoomMovesPage.jsx`, Room Moves DEV API rows and warning normalization | completed | Runtime table and drawer behavior plus DEV handler tests guard scheduled timestamps, `None` display, automated primary-conflict drawer focus, and per-person bulk warning text; source PEN removes the redundant heading and stale column labels before `npm run pen:sync`; browser evidence is saved under `artifacts/issues/issue-54-134-135-137/` |
| UIH-087 | Shared implemented pages | GitHub issue #59: page-level last-refreshed, manual refresh, and manual sync affordances need one shared primitive so Data Quality and Student Data Cleanup do not drift in copy, sizing, loading state, or accessible names | docs/new behavior, runtime behavior | `docs/product/product-requirements.md`, `docs/planning/implementation-plan.md`, `docs/design/mocks/wireframes/implemented-page-design-contract.md`, `sharedShellPresentation.jsx`, intentional page callers | open | Shared page sync/refresh primitive contract is defined in docs; remaining work is the shared runtime implementation, `npm run build:web`, `npm run a11y:check`, `npm run docs:comments:check`, and runtime screenshot evidence if a local app can run |
| UIH-086 | Frequent Fliers | GitHub issue #58: date range should be configurable instead of hardcoded to last 90 days in the live runtime copy and filters | docs/new behavior, runtime behavior | `docs/product/product-requirements.md`, `docs/planning/implementation-plan.md`, `FrequentFliersPage.jsx` | completed | PRD/plan behavior update, runtime-owned date range selector, table filtering and drawer rule context, `npm run build:web`, `npm run a11y:check`, and Browser evidence on `/frequent-fliers`; drawer-height primitive is satisfied by the merged shared drawer issue #40 |
| UIH-087 | Onboarding | GitHub issue #41: manual intake validation feedback should stay quiet until the operator clicks Save, the phone field should format and validate as a United States phone number, the Add Non-Escape action should remain Vegas Gold, and KPI helper subtitles should be removed | runtime behavior, `.pen layout` | `wireframe-onboarding-dashboard.pen`, generated onboarding artboard, Onboarding runtime page, DEV onboarding API validation | completed | Source PEN KPI cleanup plus runtime save-attempt guard, phone formatting, backend phone validation, `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, targeted Go handler test, and headless Chrome evidence at `artifacts/issues/issue-41/onboarding-manual-intake.png` |
| UIH-088 | Offboarding and Reports | GitHub issue #42: security-risk account issues belong under Reports for IT Admin review, not on the HR Offboarding page | docs/new behavior, `.pen layout`, runtime behavior, DEV mock API behavior, pipeline | `docs/product/product-requirements.md`, `docs/planning/implementation-plan.md`, `wireframe-offboarding-dashboard.pen`, Reports runtime page, Offboarding/Security Issues DEV APIs, route registry | completed | PRD/plan route and ownership update, Offboarding source PEN security card removal through `npm run pen:sync`, DEV handler tests for Offboarding filtering and `/reports/security-issues`, `npm run build:web`, `npm run a11y:check`, `npm run pen:check`, `npm run pen:lint`, route-registry tests, and runtime HTTP evidence on `/offboarding`, `/reports`, and `/reports/security-issues`; Browser screenshots were not captured because the Browser tool is unavailable in this session |
| UIH-089 | All logged-in implemented pages | GitHub issue #89: shared sidebar active navigation highlight must align with icon, label, disclosure, and nested route row centers | `.pen layout`, runtime behavior, pipeline | `wireframe-shared-shell.pen`, shared shell sync manifest, `sharedShellPresentation.jsx` | open | Shared sidebar source row plus runtime row-center metrics, `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and route/sidebar verification on representative logged-in pages |
| UIH-090 | All logged-in implemented pages | GitHub issue #95: documented nested sidebar routes, including `/admin/feature-flags`, need subordinate route buttons that inherit role filtering and active-state behavior | `.pen layout`, runtime behavior, pipeline | `wireframe-shared-shell.pen`, route registry, shared shell sync manifest, generated artboards | open | Route-registry child route tests, shared-shell nested-route source rows, sync-generated manifest/artboards, direct-route 403 behavior through existing `session.allowed_routes`, `npm run build:web`, and `npm run a11y:check` |
| UIH-091 | Shared implemented pages | GitHub issue #97: page help drawers should use the same bounded shared right-drawer primitive as row/detail drawers instead of drifting in placement or sizing | runtime behavior | Shared help overlay and `RuntimeDrawer` primitive | closed | Shared help now passes the canonical bounded right-drawer geometry into `RuntimeDrawer`, the drawer primitive owns initial and restore focus behavior for help and row/detail callers, and verification includes `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on the requested implemented routes |
| UIH-092 | Security Issues | GitHub issue #92: the row drawer's top security issue callout sits too close to the status/detail rows below it | runtime behavior | `SecurityIssuesReportPage.jsx`, page-local CSS scoped to the shared `RuntimeDrawer` instance | closed | Security Issues keeps the shared bounded right-drawer primitive and adds only page-scoped spacing between `.security-issues-runtime__warning` and the following `RuntimeDetailList`, guarded by `npm run build:web`, `npm run a11y:check`, `npm run pen:check`, `npm run pen:lint`, and `git diff --check` |
| UIH-093 | Phone Directory | GitHub issue #98: normalize live Phone Directory columns so By Room and By Department omit redundant Details columns, every mode orders Phone before Extension, and phone/extension values remain dialable links while directory focus only changes ordering | runtime behavior, `.pen layout`, pipeline | `PhoneDirectoryPage.jsx`, Phone Directory runtime CSS, existing Phone Directory PRD/plan contract | completed | Existing docs define district-wide eligibility, phone-before-extension ordering, and `tel:` link semantics; live runtime tables own visible results while static generated table nodes stay hidden, guarded by `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on all three Phone Directory modes |
| UIH-094 | Login | GitHub issue #96: DEV login should match the revised three-element public screen and avoid stale loading route copy before authentication completes | `.pen layout`, runtime behavior, pipeline | `wireframe-login.pen`, generated login artboard, `LoginPage.jsx`, `app.jsx` login route state | closed | Redundant source PEN background child removed so the artboard root owns the white canvas; login artboard is imported synchronously and rendered while DEV session state is still loading; guarded by `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and headless `/login` verification |
| UIH-095 | Onboarding | GitHub issue #102: Onboarding manual intake must use the Vegas Gold Add Non-Escape action, the shared right-hand drawer, delayed Save-attempt validation feedback, personal phone capture/format validation, and no stale KPI helper subtitles | runtime behavior, `.pen layout`, pipeline | `wireframe-onboarding-dashboard.pen`, generated onboarding artboard, `OnboardingPage.jsx`, DEV onboarding manual-draft APIs, shared `RuntimeDrawer` primitive | closed | Current `ui-improvements` already carries the source PEN KPI cleanup, hidden static Add button nodes, runtime-owned Vegas Gold Add action, shared drawer dependency from issue #97, manual Save-attempt validation guard, personal phone formatting/validation, and targeted DEV handler tests; this row links #102 to those page-specific guards and the verification set: `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, focused Go handler tests, and browser evidence on `/onboarding` |
| UIH-157 | Onboarding | GitHub issue #157: the runtime-owned Add Non-Escape button regressed to native/default styling because its CSS referenced an undefined Vegas Gold token | runtime behavior | Onboarding runtime button styling in `frontend/src/styles.css` | closed | Runtime CSS now uses the documented `var(--color-highlight)` Vegas Gold token with explicit hover, active, and focus-visible preservation; guarded by `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/onboarding` |
| UIH-096 | Student Data Cleanup | GitHub issue #91: visible leading/trailing whitespace markers, site-secretary-friendly current/suggested labels, unchanged suggested-name suppression, and the base Aeries link must stay page-local without duplicating shared drawer-scroll work from #110 | runtime behavior, `.pen layout`, pipeline | `wireframe-site-secretary-student-data-cleanup.pen`, generated Student Data Cleanup artboard, `StudentDataCleanupPage.jsx`, page-local display helper tests, `docs/testing/test-matrix.md`, `docs/planning/implementation-plan.md` | completed | Source PEN labels updated before `npm run pen:sync`; runtime values render `·` edge-space markers in the table and drawer, unchanged suggested drawer rows are suppressed, the Aeries action uses `https://windsorusd.aeries.net/admin/Default.aspx`, and helper tests plus `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, and `npm run a11y:check` guard the behavior |
| UIH-097 | Departing Seniors | GitHub issue #100: the retained school-year selector, IncidentIQ serial links, hidden Grad Year table column, row drawer context, and page-local table grid need one guarded acceptance record | runtime behavior, `.pen layout`, pipeline | `DepartingSeniorsPage.jsx`, `styles.css`, DEV Departing Seniors API payload/tests, generated Offboarding artboard underlay | closed | Existing DEV handler tests guard retained school-year options, asset URL payloads, row mutations, and persona access; the runtime table keeps Grad Year in drawer context instead of visible columns, renders `No outstanding devices` as plain text, and uses page-local CSS columns with `10px` gaps so the row grid fits the runtime pane; guarded by `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and focused Go handler tests |
| UIH-098 | Frequent Fliers | GitHub issue #99: current `ui-improvements` already carries the page-specific filter, deterministic link, and trend-mark behavior, while the remaining no-internal-scroll drawer behavior is owned by shared primitive issue #110 | runtime behavior | `FrequentFliersPage.jsx`, `frequentFliersModel.mjs`, focused Frequent Fliers model tests | completed | Page-specific Frequent Fliers behavior is guarded by focused Node tests for default `>=2` devices in 90 days, selectable lookbacks, selected metric filtering, deterministic DEV IncidentIQ URLs, and severity trend classes; shared drawer scroll behavior remains deferred to #110 |
| UIH-099 | IT Admin Dashboard | GitHub issue #90: clean `/dashboard/it-admin` as an operational overview while preserving `/admin` as the separate IT Admin controls/configuration surface owned by issue #94 | `.pen layout`, pipeline | `wireframe-it-admin-overview.pen`, generated dashboard artboard | open | Source PEN card/table cleanup, explicit status-only global-pause copy, `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and runtime evidence on `/dashboard/it-admin` plus `/admin` route-boundary verification |
| UIH-100 | Admin | GitHub issue #94: remove unsupported `/admin` Refresh and Application Timezone artifacts, rewrite Admin help around concrete IT Admin controls, and clean Admin card spacing without touching `/dashboard/it-admin` or shared shell/drawer/scroll primitives | `.pen layout`, runtime behavior, pipeline | `wireframe-it-admin-admin-controls.pen`, generated Admin artboard, Admin help copy, static refresh metadata | completed | Source `.pen` Admin cleanup, Admin static-refresh opt-out, Admin help drawer copy, `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and browser verification on `/admin` and `/admin/feature-flags` |
| UIH-131 | All logged-in implemented pages | GitHub issue #131: shared help drawer copy must be route-specific, PRD/implementation-plan grounded, and useful as first-time operator training rather than generic page help | docs/new behavior, runtime behavior | Route help content model, shared shell help resolver, route-registry tests, shared help code-path docs | in progress | Route-keyed help content authored for implemented logged-in pages, child routes resolved by `activeRoutePath`, source notes added per route, and coverage guarded by `routeRegistry.test.mjs`; no shared drawer placement, scrolling, or unrelated artboard changes |
| UIH-101 | Data Quality | GitHub issue #101: Data Quality should read as an IT Admin district-wide awareness and escalation surface, with owner-surface routing guidance in help and no unsupported local correction shortcuts | `.pen layout`, runtime behavior | `wireframe-data-quality-dashboard.pen`, Data Quality DEV payload, Data Quality help copy, Data Quality access tests | completed | PEN table-header alignment cleanup removed the Data Quality table-baseline warning, owner-surface next-action copy replaced local correction verbs, focused DEV API tests guard IT Admin-only access and unsupported-action regression, and verification includes `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, `node --test frontend/src/lib/routeRegistry.test.mjs`, `go test ./internal/web -run 'TestDevSessionLoginLogoutAndDataQualityRoutesInDevelopment/(it_admin_login_sets_session_and_can_load_data_quality|unauthenticated_data_quality_is_401|non_it_admin_personas_cannot_directly_load_data_quality)'`, direct `403` API evidence for `site_secretary`, and headless Chrome evidence at `artifacts/issues/issue-101/data-quality-it-admin.png` |
| UIH-128 | Sync Transparency | GitHub issue #128: remove the stray unlabeled Vegas Gold title-adjacent artifact while preserving the documented shared-header Refresh control | `.pen layout`, pipeline | `wireframe-sync-transparency-dashboard.pen`, generated Sync Transparency artboard | completed | Page-local title artifact removed from the authoritative PEN, shared Refresh left in the canonical header position, and the source is guarded by `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, `git diff --check`, and browser verification on `/reports/sync-transparency` |
| UIH-129 | Sync Transparency | GitHub issue #129: apply shared table row spacing/rhythm to Sync Transparency report rows without broad shared-table edits owned by other workers | `.pen layout`, runtime behavior | `wireframe-sync-transparency-dashboard.pen`, generated Sync Transparency artboard, `StaticPenPage.jsx` row hotspots | completed | Sync Transparency rows now use the shared divider-before-row rhythm with at least 5px between row text and dividers; page-local row hotspots were moved with the PEN rows, and the fix is guarded by the standard PEN, build, accessibility, diff, and browser checks plus related-page audit |
| UIH-130 | Sync Transparency | GitHub issue #130: clean title/helper/tab/KPI/table-card overlap before narrower header or table fixes | `.pen layout`, pipeline | `wireframe-sync-transparency-dashboard.pen`, generated Sync Transparency artboard | completed | Table card and close affordance were moved below the KPI band in the authoritative PEN so bordered wrappers and table headers no longer collide; guarded by `npm run pen:sync`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, `git diff --check`, and desktop plus narrow-desktop browser verification |
| UIH-102 | All logged-in implemented pages | GitHub issue #125: shared page panes were centered separately from viewport-fixed shell chrome, creating a right-shifted inner page and gray lower canvas on long routes | runtime behavior | shared page frame CSS, implemented-page design contract, PRD/implementation plan | completed | Logged-in page frames are left-aligned to the fixed shared shell and use a white scroll-container background, guarded by `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, `git diff --check`, and representative route verification |
| UIH-103 | All logged-in implemented pages | GitHub issue #133: DEV persona switcher rendered as an out-of-bounds top-right toolbar that could distort side-anchored drawers and overlays | runtime behavior, docs/new behavior | DEV persona switcher component, shared shell CSS, PRD/implementation plan, implemented-page design contract | completed | The DEV-only switcher remains functionally unchanged but is positioned inside the shared sidebar bounds below Platform Status, guarded by source checks in `npm run a11y:check`, `npm run build:web`, and persona-switch route verification |
| UIH-126 | Departing Seniors | GitHub issue #126: align the page-local title and retained school-year selector while reducing excess table/card whitespace below the visible rows | runtime behavior | `DepartingSeniorsPage.jsx`, page-local Departing Seniors CSS | completed | Runtime pane keeps the school-year selector behavior and shared drawer intact while the page-local table card sizes to visible rows, uses bounded scrolling only when row count exceeds the visible range, and exposes a filtered-empty status; guarded by `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, focused frontend tests when available, browser verification on `/departing-seniors`, and `git diff --check` |
| UIH-150 | Login and logged-out error pages | GitHub issues #150 and #151: logged-out login/error artboard text reused short ids also used by the shared shell, causing sticky shell CSS to pin page text near the viewport origin; `/error/404` was also unreachable while logged out because the route guard rewrote it to `/error/401` | runtime behavior, `.pen layout` verification | `PenArtboard.jsx`, `sharedShellArtboard.mjs`, `routeAccess.mjs`, login/error generated artboards | closed | Shared-shell sticky behavior is gated by actual shell frames, explicit logged-out `/error/{code}` routes remain public, and focused Node tests plus `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and headless Chrome verification guard `/login` and `/error/404` |
| UIH-152 | All implemented pages with long runtime content | GitHub issue #152 and follow-up PR #179 scope correction: grey bottom fade or generated page edge appears while a route still has scrollable runtime data below | runtime behavior, pipeline | `PenArtboard.jsx`, `scripts/dev_long_page_harness.mjs`, long-table runtime routes | open | `PenArtboard` measures non-sticky runtime descendants and extends the shared artboard/page background for any route whose data rows/cards extend below the generated canvas; guarded by `npm run long-pages:self-test`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and long-page harness recipes for every logged-in implemented route |
| UIH-153 | Feature Flags | GitHub issue #153: Feature Flags runtime pane leaves excessive right-side whitespace inside the logged-in page area | runtime behavior | `FeatureFlagsPage.jsx`, Feature Flags runtime CSS | closed | Page-local runtime pane width keeps the right gutter bounded while preserving shell/header/sidebar bounds and responsive stacking; guarded by `npm run build:web`, `npm run a11y:check`, and browser verification on `/admin/feature-flags` |
| UIH-155 | All logged-in implemented pages | Reopened GitHub issue #155: the DEV persona switcher must sit directly below the full Support and Platform Status block, not above it or detached to the viewport bottom | runtime behavior | DEV persona switcher shared-shell CSS and accessibility source check | completed | The shared `.dev-toolbar` anchor now stays at the rendered shared-shell coordinate below `All Systems Operational` and removes the viewport-bottom fallback that pulled it upward on shorter Browser viewports; guarded by `npm run build:web`, `npm run a11y:check`, `npm run pen:check`, `npm run pen:lint`, `git diff --check`, and Browser verification on `/dashboard/it-admin` plus `/admin/feature-flags` |
| UIH-154 | All implemented pages with long runtime content | GitHub issue #154 and follow-up PR #179 scope correction: white page background stops before the full scroll height when route data grows beyond the static artboard | runtime behavior, pipeline | `PenArtboard.jsx`, `scripts/dev_long_page_harness.mjs`, long-table runtime routes | open | Shared artboard measurement covers long Feature Flags content and any other route whose runtime rows/cards overflow the generated page height; guarded by `npm run long-pages:self-test`, `npm run pen:check`, `npm run pen:lint`, `npm run build:web`, `npm run a11y:check`, and harness recipes that clone or inject rows on every logged-in implemented route |
| UIH-199 | All route help drawers | GitHub issue #199: operator help drawers should not expose DEV-only implementation language such as deterministic mock links, DEV data slices, or non-live provider-record caveats | runtime behavior | `frontend/src/lib/routeHelpContent.js` route-keyed help content | in progress | Help drawer copy is rewritten to describe links, refresh, staged flags, and drawer context in production-neutral operator language; guarded by route help content coverage tests, `npm run build:web`, and targeted search for rendered DEV/mock implementation wording |

## Page Index

- `admin`: active shared-scope/dropdown and empty-title-pill annotations are captured in UIH-046 and UIH-047; Feature Flags subpage scaffold cleanup is captured in UIH-071; active table-baseline lint warnings are captured in UIH-074; issue #61 Admin artboard alignment and invalid-artifact cleanup is captured in UIH-082; shared-sidebar active alignment and nested Admin `/admin/feature-flags` navigation are captured in UIH-089 and UIH-090; issue #97 shared help drawer geometry is captured in UIH-091; issue #94 Admin Refresh, help, timezone-card, and card-spacing cleanup is captured in UIH-100; Feature Flags right gutter is captured in UIH-153, while the full-height background/fade class of defects is escalated to all implemented pages in UIH-152 and UIH-154.
- `dashboard-hr-lifecycle`: no additional active annotations captured yet.
- `dashboard-it-admin`: active table-baseline lint warnings are captured in UIH-074; issue #90 dashboard overview cleanup and `/admin` split preservation are captured in UIH-099.
- `dashboard-site-admin`: active table-baseline and helper-paragraph lint warnings are captured in UIH-074 and UIH-075.
- `data-quality`: see `docs/design/mocks/wireframes/data-quality-annotation-ledger.md`; active table-baseline lint warnings are captured in UIH-074; issue #57 help routing and mapping-dashboard validation is captured in UIH-083; issue #97 shared help drawer geometry is captured in UIH-091; issue #101 IT Admin awareness and owner-surface routing cleanup is captured in UIH-101.
- `departing-seniors`: current senior-class retirement and device-return behavior is captured in UIH-027, UIH-084, and UIH-097; issue #126 page-local title, school-year selector, and table/card whitespace cleanup is captured in UIH-126.
- `frequent-fliers`: active table-baseline lint warnings are captured in UIH-074; page-local hidden generated-node debt is captured in UIH-077; issue #58 configurable lookback behavior is captured in UIH-086; issue #97 shared help drawer geometry is captured in UIH-091; issue #99 page-specific filter/link/trend regression coverage is captured in UIH-098, while shared drawer scrolling remains owned by issue #110.
- `login`: accepted text-overflow lint false positive is captured in UIH-073; issue #53 error-status consistency is captured in UIH-084; issue #96 three-element public login contract is captured in UIH-094.
- `my-profile`: active table-baseline and helper-paragraph lint warnings are captured in UIH-074 and UIH-075.
- `onboarding`: active table-baseline lint warnings are captured in UIH-074; page-local hidden generated-node debt is captured in UIH-077; issue #41 manual intake validation and KPI cleanup is captured in UIH-087; issue #102 manual intake drawer/Add action verification is captured in UIH-095; issue #157 Add Non-Escape Vegas Gold runtime styling is captured in UIH-157.
- `offboarding`: page-local hidden generated-node debt is captured in UIH-077; issue #42 security-risk relocation to Reports is captured in UIH-088.
- `phone-directory-by-department`: active table-baseline and wrapper/card/rail lint warnings are captured in UIH-074 and UIH-076; page-local hidden generated-node debt is captured in UIH-077; issue #56 runtime column/link feedback is captured in UIH-078; issue #98 column/link contract verification is captured in UIH-093.
- `phone-directory-by-person`: active shared-header scope dropdown primitive reuse is captured in UIH-046; active table-baseline lint warnings are captured in UIH-074; page-local hidden generated-node debt is captured in UIH-077; issue #56 runtime column/link feedback is captured in UIH-078; issue #98 column/link contract verification is captured in UIH-093.
- `phone-directory-by-room`: active table-baseline lint warnings are captured in UIH-074; page-local hidden generated-node debt is captured in UIH-077; issue #56 runtime column/link feedback is captured in UIH-078; issue #98 column/link contract verification is captured in UIH-093.
- `reports`: active table-baseline lint warnings are captured in UIH-074; latest runtime drawer feedback is captured in UIH-044; page-local hidden generated-node debt is captured in UIH-077; issue #42 Security Issues report relocation is captured in UIH-088; issue #228 pending Zoom Desk Phone rename report is captured in UIH-228.
- `reports-sync-transparency`: issue #128 title artifact cleanup, issue #129 shared table row spacing, and issue #130 top-of-page overlap cleanup are captured in UIH-128, UIH-129, and UIH-130.
- `reports-ticketing-human-work`: active table-baseline and helper-paragraph lint warnings are captured in UIH-074 and UIH-075.
- `room-moves`: latest cancel/reversal behavior feedback is captured in UIH-052; latest bulk-draft room selector cleanup is captured in UIH-053; latest main-page table/action cleanup is captured in UIH-054; latest bulk-row author/table feedback is captured in UIH-055; latest bulk-row navigation and action-placement feedback is captured in UIH-056; latest empty-room phone outcome feedback is captured in UIH-057; latest action-label/mode feedback is captured in UIH-058; latest bulk draft helper-copy cleanup is captured in UIH-062; primary-room conflict automation context is captured in UIH-081; bulk draft table layout and add/removal semantics are captured in UIH-085; page-local hidden generated-node debt is captured in UIH-077; issue #97 shared help drawer geometry is captured in UIH-091.
- `student-data-cleanup`: active shared-header scope dropdown annotation is captured in UIH-046; latest legacy invalid-name mock coverage is captured in UIH-045; page-local hidden generated-node debt is captured in UIH-077; issue #60 friendly terminology, Aeries base-link behavior, and runtime whitespace cleanup are captured in UIH-079; issue #97 shared help drawer geometry is captured in UIH-091; issue #91 name-diff marker behavior is captured in UIH-096.
