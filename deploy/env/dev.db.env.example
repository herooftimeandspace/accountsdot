# Copy to deploy/env/dev.db.env on the remote QEMU host.
# This database is intentionally long-lived across app redeploys.
POSTGRES_DB=provisioner_dev
POSTGRES_USER=provisioner_dev
POSTGRES_PASSWORD=replace-with-generated-dev-db-password
