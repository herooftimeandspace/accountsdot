# Copy to deploy/env/staging.db.env on the remote QEMU host.
# This database is intentionally long-lived across app redeploys and should be
# loaded only with sandbox or masked production-derived data.
POSTGRES_DB=provisioner_staging
POSTGRES_USER=provisioner_staging
POSTGRES_PASSWORD=replace-with-generated-staging-db-password
