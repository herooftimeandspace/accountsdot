# Copy to deploy/env/main.db.env on the remote QEMU host if main uses the
# compose-managed database service. Use an encrypted host volume or managed
# encrypted storage for production data.
POSTGRES_DB=provisioner_main
POSTGRES_USER=provisioner_main
POSTGRES_PASSWORD=replace-with-generated-main-db-password
